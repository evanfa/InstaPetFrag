<?xml version="1.0" encoding="utf-8"?>
<android.support.v7.widget.Toolbar
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/tToolBar"
    android:layout_width="match_parent"
    android:layout_height="?attr/actionBarSize"
    android:background="@color/colorPrimary"
    android:theme="@style/ThemeOverlay.AppCompat.ActionBar"
    app:popupTheme="@style/ThemeOverlay.AppCompat.Light">

    <!-- android:elevation="@dimen/actionBarSize"-->

    <android.support.v7.widget.ActionMenuView
        android:id="@+id/actionMenuView"
        android:layout_height="?attr/actionBarSize"
        android:layout_width="wrap_content"
        />
<!--
    <ImageView
        android:id="@+id/returnPet"
        android:layout_width="@dimen/barSize36"
        android:layout_height="@dimen/barSize36"
        android:layout_gravity="center_vertical"
        android:layout_weight="30"
        android:src="@drawable/doggyhome2"
        />-->

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:text="@string/app_name"
        android:textColor="@color/textPrimary"
        android:textSize="@dimen/titleName26"
        android:textStyle="bold"
        android:gravity="center"
        android:paddingLeft="@dimen/barSize50"
        />
</android.support.v7.widget.Toolbar>