<?xml version="1.0" encoding="utf-8"?>
<android.support.v7.widget.Toolbar
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="?attr/actionBarSize"
    android:background="@color/colorPrimary"
    android:elevation="@dimen/actionBarSize"
    android:theme="@style/ThemeOverlay.AppCompat.ActionBar"
    app:popupTheme="@style/ThemeOverlay.AppCompat.Light">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        >

        <ImageView
            android:id="@+id/manoMascota"
            android:layout_width="@dimen/barSize36"
            android:layout_height="@dimen/barSize36"
            android:layout_gravity="center_vertical"
            android:src="@drawable/doggyhome2" />

        <TextView
            android:id="@+id/tituloToolbar"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical"
            android:text="@string/app_name"
            android:textSize="@dimen/titleName28"
            android:textColor="@color/textPrimary"
            android:textStyle="bold"
            android:layout_weight="60"
            android:gravity="center" />

        <ImageButton
            android:id="@+id/btnStar"
            android:layout_width="@dimen/titleName45"
            android:layout_height="@dimen/titleName45"
            android:layout_gravity="center_vertical"
            android:background="@drawable/star5"
            />
    </LinearLayout>
</android.support.v7.widget.Toolbar>